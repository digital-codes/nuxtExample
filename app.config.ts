export default defineAppConfig({
    ui: {
        strategy: 'override',
        button: {
            color: {
                white: {
                    solid: 'bg-white dark:bg-gray-900'
                }
            }
        },
        slideover: {
            width: "max-w-60 md:max-w-sm"
        }
    }
})
